---
title: "Commands"
description: "Available commands in TS Flood."
icon: "terminal"
---

## /ts_flood_open

```txt
/ts_flood_open
```

Opens the flood menu. Shows the current level, target level, preset modes, and options to set or force a custom level.

The same action is registered as a key mapping (`Open TS Flood menu`, default: `F1`).
