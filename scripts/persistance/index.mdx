---
title: "Persistence"
description: "Keep your vehicles exactly where they belong."
---
This resource allows vehicles to remain persistent in the world, ensuring they stay where they were last left.

Follow the steps below to install and configure the script properly.


---

## 1. Requirements

This script relies on the following dependencies. Make sure they are installed **before** running the resource:

- **[ox_lib](https://github.com/communityox/ox_lib/releases)**  
  Installation guide: [https://coxdocs.dev/ox_lib](https://coxdocs.dev/ox_lib)

- **[oxmysql](https://github.com/communityox/oxmysql/releases)**  
  Installation guide: [https://coxdocs.dev/oxmysql](https://coxdocs.dev/oxmysql)
---
---

## 2. Framework Support

A framework is required for this script to function. The following frameworks are supported:

- **[QBCore](https://github.com/qbcore-framework/qb-core)**
- **[QBox](https://github.com/Qbox-project/qbox-core)**
- **[ESX](https://github.com/esx-framework/esx_core)**
- **Custom framework** (manual integration required) See [Custom Framework](/scripts/persistance/custom-framework).

You must define the framework you are using in the `config.lua` file.

```lua shared/config.lua
Config.Framework = "qb" -- "qb", "esx", "custom"
```

## 3. Installation

1. Add the script to your `resources` folder.
2. Add `ensure persistance` to your `server.cfg`.


```js server.cfg
ensure <your_framework>

ensure ox_lib
ensure oxmysql

ensure ts-persistance // [!code ++] You need to add this line
```

## Files Structure

<Tree>
  <Tree.Folder name="ts-persistance" defaultOpen>
    <Tree.Folder name="server" defaultOpen>
      <Tree.File name="db.lua" />
      <Tree.File name="events.lua" />
      <Tree.File name="init.lua" />
      <Tree.File name="main.lua" />
      <Tree.File name="utils.lua" />
    </Tree.Folder>
    <Tree.Folder name="shared" defaultOpen>
      <Tree.File name="config.lua" />
      <Tree.File name="events.lua" />
    </Tree.Folder>
    <Tree.File name="fxmanifest.lua" />
  </Tree.Folder>
</Tree>
